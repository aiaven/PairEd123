<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PairEd123.Views.LoginPage"
             Title="Login">
    <VerticalStackLayout Padding="24" Spacing="12" VerticalOptions="Center">
        <Label Text="Log In" FontSize="24" HorizontalOptions="Center" />
        <Entry Placeholder="Email" Text="{Binding EmailAddress}" Keyboard="Email" />
        <Entry Placeholder="Password" IsPassword="True" Text="{Binding Password}" />
        <Button Text="Log In" Command="{Binding LoginCommand}" />
        <Button Text="Don't have an account? Register" Command="{Binding GoToRegisterCommand}"
                BackgroundColor="Transparent" TextColor="Blue" />
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        <Label Text="{Binding StatusMessage}" TextColor="Red" HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>